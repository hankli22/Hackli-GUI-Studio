# gradle

.gradle/
build/
out/
classes/

# eclipse

*.launch

# idea

.idea/
*.iml
*.ipr
*.iws

# vscode

.settings/
.vscode/
bin/
.classpath
.project

# macos

*.DS_Store

# fabric

run/
build.log

# release packages (built by build-release.cmd, large binaries)
release/

# portable editor data (projects/layouts/templates/exports created at runtime;
# run-gl.cmd points -Dhackli.home here, so it must never be committed)
data/

# generated by the editor when its data folder happens to be the working
# directory (a `run-gl.cmd --render` probe did exactly that once)
/export/
/templates/
